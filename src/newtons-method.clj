;; gorilla-repl.fileformat = 1

;; **
;;; # Newton's Method
;;; 
;;; Suppose @@f@@ has a root of multiplicity @@p > 1@@ at @@x = \alpha@@, i.e.
;;; 
;;; $$
;;; f(\alpha) = f^\prime (\alpha) = \dots = f^{(p-1)}(\alpha) = 0, \\
;;; f^{(p)}(\alpha) \ne 0
;;; $$
;; **

;; @@

;; @@
